services:
  postgres:
    image: postgres:15
    container_name: smarttest-db
    environment:
      POSTGRES_DB: smarttest
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"